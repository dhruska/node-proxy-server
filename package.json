{
  "name": "node-proxy-server",
  "version": "1.0.0",
  "description": "A simple Node.js proxy server with a CLI",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Donald Hruska",
  "license": "ISC",
  "dependencies": {
    "request": "^2.55.0",
    "yargs": "^3.8.0"
  }
}
